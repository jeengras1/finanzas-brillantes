{
  "name": "Entorno Constitucional Finanzas Brillantes v2",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers-contrib/features/jq-snap:1": {}
  },
  "postCreateCommand": "bash -c 'sudo apt-get update && sudo apt-get install -y curl && curl -fsSL https://raw.githubusercontent.com/koyeb/koyeb-cli/master/install.sh | sh && echo \"export PATH=\\$PATH:~/.koyeb/bin\" >> ~/.bashrc'",
  "customizations": {
    "vscode": {
      "extensions": [
        "GitHub.copilot"
      ]
    }
  },
  "remoteUser": "codespace"
}
